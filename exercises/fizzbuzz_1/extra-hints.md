You'll need to use a `for` loop to iterate through the numbers 1 - 100.

Inside the loop, you'll need two conditional statements (`if` and `else if`).

You will need to use the `remainder` (aka `modulo`) operator.
